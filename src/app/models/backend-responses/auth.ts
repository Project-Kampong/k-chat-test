export interface LoginUserResponse {
  userId: string;
  username: string;
  token: string;
  tokenExpiration: string;
}

export interface RegisterUserResponse {
  userId: string;
  username: string;
  token: string;
  tokenExpiration: string;
}
